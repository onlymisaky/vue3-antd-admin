export interface User {
  bindStatus: boolean;
  channelIds: number[];
  email: string;
  id: number;
  isBinding: boolean;
  name: string;
  nickname: string;
  permissions: string[];
  phone: number;
}
